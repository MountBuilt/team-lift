{
  "name": "teamlift-scripts",
  "private": true,
  "type": "module",
  "description": "Tick tooling for the NUC (event watcher + safety timer; Aiden + push). Never served by the site.",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.115.0",
    "firebase": "^11.10.0",
    "web-push": "^3.6.7"
  }
}
